services:
  - type: web
    name: face-verification-api
    runtime: docker  # Uses Dockerfile
    env: python
    buildCommand: ""  # Docker handles builds
    startCommand: ""  # Defined in Dockerfile
    envVars:
      - key: PORT
        value: 5000
      - key: PYTHONUNBUFFERED
        value: True
